services:
  - type: web
    name: ethionex-marketplace
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ethionex_api.wsgi:application
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true